/*#include <stdio.h>

int main()
{
    stdlib.h // malloc.h
    
    
    //alocalão de memória
    
    //malloc --> tamanho inicial do vetor; retorna o primeiro endereço do indice 0 zero do vetor/ parece ser só com ponteiro
    
    // tem ---> malloc e calloc > coloca zero mem todas as posições do vetor o calloc
    
    sizeof = o tamanho // sizeof(int), rever.
    
    p = malloc (100*sizeof(int))// m- alloc, Rever isso
    
    if(p != NULL) --> //para verificar se a memoria não esta corrompida
        else
            printf("problema na alocação dinamica de memória");
            
            
    free --> free(p) libera a memoria.
    
   p = realloc(p , n ) // n números a mais para receber do vetor, o primeiro parametro é p/ saber o tamanho inicial/atual dele // Valor atual mais n
   
   int v[5] // alocação estática, não é possivel mudar seu tamanho
   
   char *sres= NULL;
   
   sres = malloc(10 * sizeof(char));
   
   //ver o modelo de alocação dinamica no sigaa, professor ira postar
   
   char *s1maiss2; 
   
   simaiss2 = funcaoPonteiro( s1, s2); // chamo a função que concatena, função tipo ponteiro
   
   printf("%s", simaiss2 );
   
    return 0;
}
*/